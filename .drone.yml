kind: pipeline
name: default

steps:
  - name: build
    image: gradle:7.4.2-jdk17
    commands:
      - ./gradlew clean build
    when:
      branch:
        include:
          - bugfix/*
          - feature/*
          - main
      event:
        include:
          - push
          - pull_request
